import { EstimationPipe } from './estimation.pipe';

describe('EstimationPipe', () => {
  it('create an instance', () => {
    const pipe = new EstimationPipe();
    expect(pipe).toBeTruthy();
  });
});
