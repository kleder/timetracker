export class YouTrackColor {
    public index: number;

    public hexColor: string;

    
    constructor(index: number, hexColor: string) {
        this.index = index;
        this.hexColor = hexColor;
    }
  }